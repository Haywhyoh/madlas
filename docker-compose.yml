services:
  web:
    image: ghcr.io/haywhyoh/madlas:latest
    container_name: madlas
    restart: unless-stopped
    ports:
      - "127.0.0.1:3002:3000"
    pull_policy: missing
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
